/**
 * ListItem.stories.jss.js
 *
 * Specific styles just for storybook examples
 */

const styles = {
  // KLUDGE: Eliminate once Typography allows span elements
  span: {
    display: "inline"
  }
};

export default styles;
